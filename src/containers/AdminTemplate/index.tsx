import React, { Fragment } from 'react'

const UserTemplate: React.FC = (props) => {
    return (
        <Fragment>
            admin template
        </Fragment>
    )
}

export default UserTemplate